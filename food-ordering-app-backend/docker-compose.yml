version: '3.8'
services:
  auth-service:
    build: ./auth-service
    ports:
      - "3000:3000"
    networks:
      - food-network

  api-gateway:
    build: ./api-gateway
    ports:
      - "8080:8080"
    depends_on:
      - auth-service
    networks:
      - food-network
    environment:
      - NODE_ENV=development
    restart: unless-stopped

  

networks:
  food-network:
    driver: bridge